/**
 * What every formatter of this module writes when it has nothing to show — a
 * missing value, or one that is not a finite number. An en dash, so an empty
 * cell of a table still reads as a cell.
 */
export const MISSING_VALUE = '–';
