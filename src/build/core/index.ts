export type { BuildInfo } from './buildInfo.ts';
export {
  UNRELEASED_VERSION,
  buildLabel,
  buildStamp,
  buildSummary,
  formatBuiltAt,
  releasedVersion,
  shortCommit,
} from './buildInfo.ts';
